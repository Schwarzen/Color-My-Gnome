[tool.pytest.ini_options]
import-mode = "importlib"
pythonpath = [
    "."
]
testpaths = ["colormydesktop/tests"]

# Automatically force headless environments for every single test execution
env = [
    "A11Y_FORCE_STATE_SET=1",
    "GDK_BACKEND=headless",
    "GTK_A11Y=none"
]

